2026-07-11 08:56:00

## git commit
[update].[style] #755 [公文製作].[RWD] 行動裝置自動「頁寬」完整呈現 A4，上方選單可收合為單一 icon 並支援自動收起與滑出動畫。
- `docRules/editorZoomControl.ts`：新增 `shouldAutoFitWidth`（頁寬倍率 <100% 才自動頁寬）、`isCompactViewport`（≤640px）、`FIT_WIDTH_MARGIN_COMPACT_PX = 16`；`computeFitZoomPct` 增可選留白參數（預設 48 向後相容）＋ 3 條單元測試。
- `DocumentEditor.vue`：首次「無創稿→有創稿」若畫布容不下 100% A4 自動進頁寬模式（僅首次、之後尊重使用者選擇）；頁寬留白依視口切換 48/16px；`.editor-page` 補 `100dvh` 修正行動瀏覽器網址列遮擋。
- `EditorToolbar.vue`：工具列改 `overflow-x: auto` 橫向捲動＋ `.toolbar-inner` margin auto 安全置中（原 flex 置中於溢出時左緣不可及）；按鈕 `flex-shrink: 0`、`touch-action: manipulation`；行動版緊湊間距。
- 新增 `docRules/toolbarCollapseControl.ts`：上方選單自動收合純運算判定（`TOOLBAR_AUTO_COLLAPSE_MAX_PX = 600`、`isAutoCollapseViewport` 嚴格 <600 才收）＋ 3 條單元測試；與 640px RWD 排版斷點刻意分離（語意不同不合併）。
- `EditorToolbar.vue`：收合鈕以 `position: sticky; left: 0` 釘於工具列最左側（橫向捲動時恆可及）、圖示 `bi-chevron-left`（向左收起）；收合／展開加入 `Transition` 動畫（`toolbar-slide` 選單向左滑出＋max-height 同步收攏、`fab-pop` 浮動展開 icon 自左彈入彈出），皆尊重 `prefers-reduced-motion`；自動切換僅於「跨越」600px 門檻時觸發，不受同側 resize 干擾。
- `DraftList.vue` / `EditorZoomControl.vue`：≤640px 垂直堆疊（縮放控制升至 bottom 56px、清單貼底排）＋ `env(safe-area-inset-bottom)`；`index.html` viewport 加 `viewport-fit=cover`。
- `DocumentBody.vue`：行動版畫布留白調整（避開兩排浮動元件）；`NewDraftModal` 寬改 `min(480px, 100vw−32px)`；`PrintPreview` 補 100dvh、工具列橫向捲動、footer flex-wrap（@media print 不受影響）。
- 同步更新 `skills/business-logic/editor-zoom-control/SKILL.md`（新增 §六 RWD）與 `skills/SKILLS_INDEX.md`。
- 測試：vitest 246 綠（4 紅為既有 DI 測試、無關）、vite build 成功；RWD 行為與動畫需 npm run dev 裝置模擬實機驗收。

## prompt 1
優化 公文製作 RWD，讓其在行動裝置上也可完整呈現，並方便編輯。

## prompt 1 response
- 探索前端結構（DocumentEditor / EditorToolbar / DocumentBody / DraftList / EditorZoomControl / PrintPreview / NewDraftModal），確認 A4 固定 21cm 於手機視口會溢出。
- 選擇復用既有「頁寬」縮放基礎設施（純視覺 transform:scale、MUST 不觸發分頁重算之不變式全數保留），而非另寫行動版版面，風險最小。
- `docRules/editorZoomControl.ts` 新增 `shouldAutoFitWidth`／`isCompactViewport`／`FIT_WIDTH_MARGIN_COMPACT_PX`；`DocumentEditor.vue` 於首次有創稿時若畫布容不下 100% A4 自動進頁寬模式（僅首次、之後尊重使用者選擇）。
- `EditorToolbar.vue` 工具列改橫向可捲動＋安全置中；`DraftList` 與 `EditorZoomControl` 於 ≤640px 垂直堆疊避免重疊，加 safe-area；`.editor-page`／`PrintPreview` 補 100dvh；`NewDraftModal` 寬度流體化；`index.html` 加 viewport-fit=cover。
- 新增 3 條 RWD 單元測試；vitest 243 綠（4 紅為既有 DI 測試無關）、vite build 成功；更新 SKILL.md §六 與 SKILLS_INDEX.md。

## prompt 2
上方選單 可收起為一個 icon，當螢幕小於 600px，自動收起。

## prompt 2 response
- 新增 `docRules/toolbarCollapseControl.ts`：`TOOLBAR_AUTO_COLLAPSE_MAX_PX = 600`、`isAutoCollapseViewport`（嚴格 <600 才收），與既有 640px RWD 斷點刻意分離（語意不同不合併），附 3 條單元測試。
- `EditorToolbar.vue` 收合狀態下整條選單（含符號／螢光筆 bar dock）隱藏，僅留 fixed 左上角圓形浮動展開 icon（`bi-list`，與 DraftList／ZoomControl 浮動元件同語彙）。
- `onMounted` 依初始視口寬決定收合狀態，`resize` 監聽做「跨越門檻」切換（同側 resize 如行動瀏覽器網址列收放不觸發），尊重使用者手動收合／展開。
- vitest 246 綠、vite build 成功；更新 SKILL.md §六與 SKILLS_INDEX 補充 600px 自動收合說明。

## prompt 3
收合選單，請改到最左側，並指向左邊箭頭，且會有收起動畫。

## prompt 3 response
- 收合鈕自置中按鈕群組移出，改為 `.toolbar-bar` 首子元素並以 `position: sticky; left: 0` 釘於最左側，工具列橫向捲動時恆可及；圖示由 `bi-chevron-up` 改 `bi-chevron-left`，方向與收起動畫一致。
- 新增兩組 Vue `Transition` 動畫：`toolbar-slide`（整條選單含 bar dock 向左滑出＋max-height 同步收攏、編輯區平滑上移不瞬跳）、`fab-pop`（左上浮動展開 icon 自左縮小處彈入／彈出），兩者同時進行銜接無空窗，皆加 `prefers-reduced-motion` 停用。
- vitest 246 綠、vite build 成功；更新 SKILL.md §六描述收合鈕位置、箭頭方向與動畫機制。
