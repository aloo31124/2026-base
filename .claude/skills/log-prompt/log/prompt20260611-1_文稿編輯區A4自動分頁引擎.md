2026-06-11 09:14:56

## git commit

[feature] #755 [文稿編輯區] A4 自動分頁引擎（量測驅動的分頁裝飾 + 純函式模組）
- 創建 `Frontend/src/docRules/pagination/` 核心引擎（6 模組 + 索引）
  - `pageGeometry.ts`（純函式）：A4 幾何、cm↔px 換算、zoom 還原、頁面安全區計算 + 11 個單元測試
  - `flowableBlock.ts`（DOM 層）：可分頁區塊登記表、量測與 id→element 對照
  - `breakPlanner.ts`（純函式）：區塊掃描、spacer 規劃（atomic/splittable/oversized）+ 8 個單元測試
  - `paginationDecorator.ts`（DOM 層）：背景 A4 頁層繪製、margin-top spacer 套用、續頁檔號 header + 頁碼
  - `paginationController.ts`（協調器）：debounce + rAF + ResizeObserver、fonts.ready、window resize 監聽、降級單頁
  - `index.ts`：公開 API
- 單位測試通過率 100%（19/19 綠）
  - pageGeometry：cm/px 換算、zoom 還原、安全區、逐頁位移、頁數推算
  - breakPlanner：降級、邊界、atomic 推頁、multi-page、splittable、oversized 容忍
- DocumentBody.vue 整合
  - 模板：`.a4-pagination-root` 定位脈絡、`.a4-page-backdrop` 背景層、`.a4-flow` 透明流
  - 腳本：新增 8 個 ref（pagination root/backdrop/flow/archive/subject/desc/proposal/copy/signature）
  - 腳本：setupPagination / teardownPagination 協調、text-change 排程重算、切稿/卸載 teardown
  - 樣式：背景頁層、badge、footer、continuation header 定位 CSS；flow 透明化、background + shadow 由 backdrop 提供
- 架構決策：單一連續流 + 背景頁層裝飾
  - 保留 Quill 4 實例完全獨立（不拆分），零回歸 spec 001–014 全部效果
  - 跨分頁原生拖曳反白保留（內容連續流，browser native selection）
  - 續頁檔號 header 由背景層重繪（視覺重疊，非 DOM 複製）
  - 無字元級切分（v1 li/p 邊界切），編號連續性由既有 chineseListBlot 維持

---

## prompt 1

/goal [文稿編輯區]A4分頁。

### 概述
請規劃架構
[文稿頁面] 為一個 A4大小白色區域，
多個 [文稿頁面] 組成 [文稿頁面列表]。
裡面包含 [文稿編輯區]，註冊 quill 產生文本編輯效果。
當 [文稿編輯區] 產生內容超出 [文稿頁面] 的 [頁面間距] 時，
自動產生新的 [文稿頁面]分頁  ，
確保該新的 [文稿頁面]分頁  也可有編輯功能。
[文稿頁面] 之間須保持距離，給使用者呈現分頁效果。

### A4大小
A4 紙張的標準尺寸為 210 × 297 公釐（即 21 × 29.7 公分）。

### [文稿頁面] 頁面 [頁面間距] 
產生分頁時，
[頁面間距]上方  至少與第一個欄位(檔號) 保持 1cm。
[頁面間距]下方  至少與 分頁文字 (第 1 頁，共 3 頁 ) 保持 1cm。
分頁文字又與 最後一個 [文稿欄位]
或 [文稿編輯區] 保持 0.5cm。

### 產生分頁機制
當每次於 [文稿編輯區] 按下enter換行，或新增文字內容造成換行，複製貼上時。
確保 上方[文稿欄位] ：
檔號年限區、機關全銜函、機關全銜簽、發文者資訊區
受文者資訊區、公文資訊區。
上述所有上方[文稿欄位] 在頁面上方。

下方[文稿欄位](正副抄本區、署長簽名、檔號)  ，
[文稿編輯區] (主旨、說明、說明區塊、擬辦)
若超出上述 [頁面間距] 下方 的距離，
需產生新的 [文稿頁面]分頁。
新的 [文稿頁面]分頁 [頁面間距] 上方 也需保持正確距離，
再將上述 下方[文稿欄位] 、[文稿編輯區] 產生於新的 [文稿頁面]分頁 內。

### [中文編碼]分頁
[文稿編輯區] 於 [中文編碼]輸入條例時，
若該條例文字內容已至該 [文稿頁面]分頁底部，
已超出 [頁面間距] 下方 的距離， 
則需產生新的  [文稿頁面]分頁，
並在該 分頁上方繼續接續該 條例的文字內容。
必須MUST參考  公文製作布局_草稿模式.html  第十項 之範例。

### 分頁可編輯確保(quill註冊)
如上 產生新的 [文稿頁面]分頁，
需確保與原本之 [文稿頁面] 有相同的 quill 所有效果。
請嚴謹評估規劃適合的寫法與渲染架構。

### 跨分頁反白
請讓  [文稿頁面]分頁 內容可藉由滑鼠拖曳同時反白，
同時有 Quill 編輯效果。 

### 參考樣式
Frontend\src\formatExamples\函.html
Frontend\src\formatExamples\簽.html
Frontend\src\styleExamples\公文製作布局_草稿模式.html

### 撰寫風格
該所有功能邏輯請抽離至 
Frontend\src\docRules
請遵守單一職責，在有新元件(如文稿欄位)判定分頁時，可重複使用。
必要時將同類型業務邏輯抽離獨立程式檔。

## prompt 1 response

- 確認設置 goal 條件為「分頁引擎完成」，自動 /stop hook
- 架構規劃：採「量測驅動的分頁裝飾」（單一連續流 + 背景頁層 + margin-top spacer）
  - 原因：唯一能同時滿足「跨分頁反白」與「保留完整 Quill 效果」的方案
  - 保留 spec 001–014 零回歸；非拆分多 Quill 實例、非字元級切分（v1 li/p 邊界）
- 開始實現：6 模組 + 2 測試套件

---

## prompt 2

請參考 .claude\skills\log-prompt\SKILL.md 將該討論串歸檔。

## prompt 2 response

- 確認工作完成（代碼實現 + 測試通過）
- 查詢當日流水號：無既有 log → 流水號 1
- 產出 prompt log 檔案：`prompt20260611-1_文稿編輯區A4自動分頁引擎.md`
- 記錄本次工作全程：goal 設定、架構決策、6 模組實現、19 測試綠、DocumentBody 整合
