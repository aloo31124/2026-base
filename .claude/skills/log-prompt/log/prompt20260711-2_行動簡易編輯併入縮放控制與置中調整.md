2026-07-11 15:35:00

## git commit
[feature] #755 [公文製作] 右下角新增[行動簡易編輯]，函／簽一鍵切為簡易版面（主旨／說明／擬辦＋中文編號、手機放大文字），整併進[縮放控制]選單並可隨時切回完整格式。
- 新增 `Frontend/src/docRules/mobileSimpleEdit.ts`：格式映射純運算（簽→簡易含擬辦、函→簡易、簡易系列冪等）＋切換項標籤常數；`__tests__/mobileSimpleEdit.spec.ts` 5 條契約測試（含擬辦保留性、映射結果必為無裝飾區段）。
- `Frontend/src/view/DocumentBody.vue`：新增 `simpleLayout` prop——`currentSections` 依映射格式渲染、分頁引擎與顯示縮放停用、`watch(simpleLayout)` 以「快照→重建→載回」保留內容；CSS `.is-simple-layout` 全寬卡片版面、≤640px 字級放大至 23px。
- `Frontend/src/components/EditorZoomControl.vue`：選單內「頁寬」上方新增 `.zoom-simple-edit` 切換項（`simpleEditActive` prop／`toggle-simple-edit` emit）；啟用中頁寬／滑桿／預設／−＋步進停用，但控制本體與切換項維持可操作；主控列（展開狀態）改顯示「行動簡易編輯」取代縮放百分比（`justify-content:center` 群組置中，曾實作隱形佔位精準置中後依需求移除，回歸簡潔二段式）；收合狀態啟用中改為單一 `bi-phone` 圓形 icon（不顯示文字／箭頭），未啟用維持原百分比藥丸。
- `Frontend/src/view/DocumentEditor.vue`：`isMobileSimpleEdit` 為狀態單一事實來源，接線 `simpleLayout`／`zoom`／`simpleEditActive`／`toggle-simple-edit`；`EditorZoomControl` 之 `v-if` 僅視 `activeDraftId`，不再因簡易模式整個隱藏。
- 新增又刪除 `MobileSimpleEditToggle.vue`（獨立浮動按鈕，後續整併進 ZoomControl 選單而不再需要）。
- 新增 `Frontend/cypress/e2e/mobile-simple-edit.cy.ts`（12 案例，全綠）＋ `cy.toggleMobileSimpleEdit()` 自訂指令；全量 E2E 回歸 75/78（3 筆既有瑕疵無關）；Vitest 251/255（4 紅為既有 EditorToolbar DI 測試無關）；vite build 成功。
- 新增 `skills/business-logic/mobile-simple-edit/SKILL.md`，更新 `skills/SKILLS_INDEX.md` 與 `editor-zoom-control/SKILL.md`。

## prompt 1
右下角增加 [行動簡易編輯] 按鈕，點選 [行動簡易編輯] ，可讓 函、簽 等複雜格式 可切換為簡易樣式，
在手機版會放大文字，有主旨、說明、擬辦欄位，還有中文編號，方便手機編輯。

## prompt 1 response
- 探索 DocumentEditor／DocumentBody／EditorZoomControl／formats／draftStore，確認函／簽與簡易樣式共用同一組欄位鍵（subject/description/descriptionArea/proposal），採「只換渲染區段、不動欄位資料」方案。
- 新增 `docRules/mobileSimpleEdit.ts`（格式映射 `toSimpleLayoutFormat`：簽→含擬辦、函→不含）＋ 5 條 Vitest。
- `DocumentBody.vue` 新增 `simpleLayout` prop：區段映射、分頁引擎與顯示縮放停用、`watch(simpleLayout)` 快照→重建→載回保留內容；CSS 全寬卡片版面、行動裝置字級放大至 23px。
- 新增獨立浮動元件 `MobileSimpleEditToggle.vue`，於 `DocumentEditor.vue` 接線（固定右下、縮放控制正上方）。
- 新增 `mobile-simple-edit.cy.ts`（9 案例，全綠）；全量 E2E 回歸 72/75（3 筆既有瑕疵，與本次變更無關）；Vitest 全綠；vite build 成功。
- 新增 `skills/business-logic/mobile-simple-edit/SKILL.md`，更新 `SKILLS_INDEX.md` 與 `editor-zoom-control/SKILL.md`。

## prompt 2
行動簡易編輯 按鈕，納入至左下角的 大小縮放 bar 中，
並在 頁寬 按鈕上方。

## prompt 2 response
- 將獨立浮動鈕整併進 `EditorZoomControl.vue` 選單，於「頁寬」正上方新增 `.zoom-simple-edit` 按鈕（新增 `simpleEditActive` prop／`toggle-simple-edit` emit）。
- 啟用中頁寬／滑桿／預設／−＋步進加 `:disabled`；`EditorZoomControl` 之 `v-if` 改回僅視 `activeDraftId`（不再因簡易模式整個隱藏，確保切換項可及）。
- 刪除 `MobileSimpleEditToggle.vue`；新增 `cy.toggleMobileSimpleEdit()` 自訂指令，重寫 E2E 走選單路徑（9 案例全綠）。
- 全量 E2E 回歸 72/75 與整併前一致；vite build 成功（881 modules，少一顆檔案）。
- 更新 `mobile-simple-edit/SKILL.md`（架構改為寄居於 ZoomControl 選單）、`SKILLS_INDEX.md`、`editor-zoom-control/SKILL.md`。

## prompt 3
行動簡易編輯 文字 於按鈕 水平置中。
並且 點選 行動編輯 後， 右下角會顯示 行動簡易編輯 而非 % 比例。

## prompt 3 response
- 以 Cypress 截圖 + `getBoundingClientRect`/`Range` 量測驗證：選單內 `.zoom-simple-edit` 本已置中（diff=0）；真正問題在 `.zoom-value`（右下角主控列）為二段式 `[文字][箭頭]`，箭頭僅在右側使文字視覺偏左。
- 改為三段式 `[隱形佔位][文字 flex:1][箭頭]`（佔位與箭頭同寬），數學上保證文字置中與長短無關；同結構套用於收合藥丸。
- `label` computed 依 `simpleEditActive` 分支，啟用中顯示「行動簡易編輯」取代縮放百分比／頁寬（純狀態指示，操作入口仍在選單內按鈕）。
- 新增 2 條 E2E（11 案例）；全量回歸 74/77 與既有基準一致；Vitest 251/255（4 紅為既有無關測試）。
- 更新 `mobile-simple-edit/SKILL.md` 新增置中排版數學小節。

## prompt 4
行動簡易編輯 文字 於按鈕 水平垂直置中。
並且 右下角 的 縮放bar 若是 [行動簡易編輯] 縮小收合的話只顯示 一個 手機icon。

## prompt 4 response
- `.zoom-simple-edit` 新增顯式 `line-height:1`，與 icon 行高基準一致，強化垂直置中。
- 收合鈕新增條件：`isCollapsed && simpleEditActive` 時只渲染 `bi-phone` 圖示（無文字／箭頭），並掛 `zoom-collapse-btn--simple-edit` class 覆寫回 28px 圓形；未啟用時收合行為不變。
- 以 Cypress 截圖複驗：收合後確實僅顯示圓形手機圖示，展開後仍可切回完整格式。
- 新增/調整 3 條 E2E（12 案例）；全量回歸 75/78 與既有基準一致；vite build 成功。
- 更新 `mobile-simple-edit/SKILL.md`（垂直置中說明、收合圖示行為、排錯項）與 `SKILLS_INDEX.md`。

## prompt 5
右下角 收合 bar，多出
<span data-v-1c115fa9="" class="zoom-value-spacer" aria-hidden="true"></span>
會占用空間，用途為何? 請移除

## prompt 5 response
- 說明 spacer 用途（鏡射箭頭寬度以達成像素級精準置中，為前一輪需求所加），並依指示移除。
- `.zoom-value` / 收合藥丸移除 `.zoom-value-spacer` / `.zoom-collapse-spacer`，回歸簡潔二段式 `[文字][箭頭]` ＋ `justify-content:center` 群組置中（與 `.zoom-fit` 等既有按鈕同慣例，允許幾 px 可接受偏移）；收合手機圖示分支不受影響。
- 以 Cypress 截圖複驗按鈕變窄、無多餘留白；`mobile-simple-edit.cy.ts` 12/12 全綠；全量回歸 75/78、Vitest 251/255 皆與既有基準一致；vite build 成功。
- 更新 `mobile-simple-edit/SKILL.md` 記錄「曾嘗試精準置中、因空間成本已依需求回退」之決策脈絡與未來如何重新導入的作法。
