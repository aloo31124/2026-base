2026-06-09 15:58:04

## git commit
[style] #755 [公文製作] styleExamples 窄於 400px 自動收合控制並切頁寬
- 右下角比例縮放icon.html：收合邏輯抽為 setCollapsed()；新增 matchMedia(<400px) 自動收合並切「頁寬」(isFit)，變寬自動展開。
- 左下角文稿清單.html：新增 matchMedia(<400px) 以 render(true) 自動收合，變寬 render(false) 展開。
- 公文製作布局_草稿模式.html：抽出 setZoomCollapsed()，新增 applyNarrow() 於 <400px 同時收合文稿清單與縮放、頁數改頁寬，變寬還原。

## prompt 1
當螢幕寬度窄於 400px，
將
Frontend\src\styleExamples\右下角比例縮放icon.html
Frontend\src\styleExamples\左下角文稿清單.html
自動收合起。

## prompt 1 response
- 兩項屬視窗寬度響應式行為，無法純 CSS 切換（文稿清單展開/收合為不同 DOM 範本、縮放收合含 JS 狀態），統一以 window.matchMedia('(max-width: 399.98px)') 監聽 change 與初始化各觸發一次。
- 右下角比例縮放icon.html：將收合邏輯抽成 setCollapsed() 供點擊與響應式共用；<400px 自動收合、變寬自動展開。
- 左下角文稿清單.html：<400px 呼叫既有 render(true) 自動收合，變寬 render(false) 展開。
- 採 399.98px 表達「嚴格小於 400px」，避免與其他 min-width:400px 斷點邊界重疊。

## prompt 2
當螢幕寬度窄於 400px，頁數自動改為頁寬

## prompt 2 response
- 右下角比例縮放icon.html：在 <400px 的 applyNarrow() 中加入 isFit = true（頁數→頁寬）再 apply()，並一併收合；變寬時展開。
- 公文製作布局_草稿模式.html（整合檔，一併同步）：抽出 setZoomCollapsed()，新增統一 applyNarrow()——<400px 時 isFit=true（頁寬，實際套用 CSS zoom 符合畫布）＋ renderDraft(true) ＋ setZoomCollapsed(true) 三者一起收合/頁寬，變寬還原。
