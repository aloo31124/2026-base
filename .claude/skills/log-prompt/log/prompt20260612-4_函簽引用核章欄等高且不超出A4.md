2026-06-12 14:32:00

## git commit
[update] #755 [文稿範本] 函／簽引用核章欄並改固定高度等高，確保不超出 A4
- 函.html、簽.html 內嵌核章欄.html 的 CSS 與標記（標註引用來源），一比一還原虛線外框、第一層決行＋必填星號、三欄標頭與簽核空白區。
- 新增 --chop-fs:16px / --chop-lh:1.3 專屬變數，避免與範本既有 --fs-main(21.3px) 主字級衝突。
- 新增共用變數 --chop-body-h:4cm，簽核空白區由 flex 撐滿改為固定高度，使函／簽核章欄方框完全等高。
- .chop-section/.chop-box 取消 flex-grow（flex:0 0 auto），page-footer 維持 margin-top:auto 置底。
- 以可用空間較緊的函為高度上限，無頭 Edge 渲染確認兩範本均為單頁、未溢出 A4。

## prompt 1
請參考 
Frontend\src\formatExamples\核章欄.html 
將該核章欄位一比一完全一樣的樣式讓參考格式：
Frontend\src\formatExamples\函.html
Frontend\src\formatExamples\簽.html
引用該 核章欄.html 。
且確保都在 A4 範本內不超出。

## prompt 1 response
- 先閱讀 核章欄.html、函.html、簽.html 三檔與 核章欄.png，釐清核章欄結構：虛線外框＋「第一層決行（紅色必填星號）」＋「承辦單位／會辦單位／決行」三欄標頭＋大片簽核空白區。
- 因純 HTML 無 include 機制，採「內嵌相同 CSS＋相同標記、並標註引用來源」方式引用 核章欄.html，做到一比一外觀。
- 關鍵一：字級衝突。核章欄原用 --fs-main:16px，但函／簽的 --fs-main 是 21.3px（內文主字級），故另開 --chop-fs:16px、--chop-lh:1.3 專屬變數避免被放大。
- 關鍵二：A4 不超出。原固定 8cm 空白區在兩範本既有內容下會溢出，改用 flex:1 讓簽核空白區自動撐滿頁面剩餘高度、延伸至 A4 底部，頁尾自然落於其下。
- 對函.html、簽.html 各做三段編輯：root 加核章欄變數、style 加核章欄規則並調整 page-footer、HTML 於 page-footer 前插入核章欄標記。

## prompt 2
請讓 函 核章欄 與 簽 一樣高。

## prompt 2 response
- 分析：原 flex 自動撐高會因函上方欄位區段較多、剩餘空間較小，導致兩者核章欄高度不一致。
- 改為固定高度等高：新增共用變數 --chop-body-h:4cm，簽核空白區由 flex:1 改為 height:var(--chop-body-h)，函／簽套用同值 → 虛線方框完全等高。
- .chop-section/.chop-box 取消 flex-grow 改 flex:0 0 auto；page-footer 恢復 margin-top:auto 置底。
- 取 4cm 是以可用空間較緊的函為上限，確保兩者皆不超出單頁 A4。
- 用無頭 Edge（--headless=new）將兩檔渲染截圖實測，確認各為「第 1 頁，共 1 頁」、核章欄與頁尾皆在 A4 白底內、虛線方框高度一致，渲染後清除暫存截圖。
