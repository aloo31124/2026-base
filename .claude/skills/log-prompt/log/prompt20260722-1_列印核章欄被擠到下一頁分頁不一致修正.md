2026-07-22 09:17:21

## git commit
[fix] #755 [列印預覽] 修正核章欄於 Chrome 列印被擠到下一頁、與 [列印視窗] 預覽不一致。
- 根因：PrintPreview.vue 中意欲重置內距的 `.preview-scroll .preview-doc-body .editor-body`
  規則**從未生效**——`class="preview-doc-body"` 由 Vue 落在 `<DocumentBody>` 的根元素，
  而根元素本身即 `<div class="editor-body">`，兩個 class 同屬「一個」元素，
  但該選擇器用的是**子孫組合子**（中間有空白），無法匹配元素自身。
- 後果：`.editor-body` 基礎樣式 `padding: 30px 20px 96px` 一路保留到列印。
  螢幕上背景頁層是「相對 .a4-flow 定位」會把祖先內距整個吸收 → 預覽看不出問題；
  但列印以文件頂端起算 → 內容流頂端較實體頁頂多下移約 0.8cm、底部多出約 2.5cm，
  壓縮第 1 頁可用高度。「函」初始畫面即近滿版（核章欄含 4cm 簽核區、底端約 26.4cm，
  全頁僅約 3.3cm 餘裕），遂使標有 `break-inside: avoid` 的核章欄整塊被推到第 2 頁。
- 修法：`@media print` 內加 `.preview-app .editor-body { margin:0!important; padding:0!important }`，
  恢復分頁引擎「flow 頂端 ≡ 實體頁頂」之模型；螢幕預覽留白維持原樣不動（避免動到已正常的外觀）。
- 量測（以專案實際 CSS 組靜態重現頁）：.a4-flow 內容高 1033px、核章欄底端（相對 flow 頂）999px
  ≈26.4cm、引擎 usableBottom 1034px（999<1034 故預覽判 1 頁）；
  列印時整份文件高 30+1033+96=1159px > A4 1122px。
- 測試：列印＋分頁幾何單元測試 21/21 綠；全套 277/281，其中 EditorToolbar.spec.ts 之 4 筆
  經 git stash 比對確認為**既存失敗**、與本次修改無關。瀏覽器層級列印須實機 Chrome 驗收。
- 文件：print-preview-flow SKILL §二 新增第 5 點（flow 祖先鏈內距必須於 @media print 歸零，
  及「同元素多 class 不可用子孫選擇器」陷阱）；SKILLS_INDEX 修正仍在描述已廢除之
  beforeprint/getPageGapCm 舊機制的過時敘述。

## prompt 1
格式 函，在初始畫面(只有一行中文編碼)時，預覽列印畫面是正常一頁，
為何在 預覽列印視窗，按下 列印ICON 後開啟 chrom 的預覽列印視窗，
卻會讓核章欄 跑到下一個分頁，造成不一致。

請找出原因並修正問題。

## prompt 1 response
- 研讀 PrintPreview.vue（含 @media print 全區）、DocumentBody.vue、ChopSection.vue、
  paginationController / paginationDecorator / pageGeometry，確認現行架構為
  「`[data-a4-page-start]` + `break-before: page`」強制斷頁（非已廢除的 beforeprint 收 pageGap）。
- 判定單頁情境下**不會**產生 `data-a4-page-start`（無 spacer）→ 列印純靠自然流，
  故問題必來自「flow 頂端在實體頁中的位置」或「頁高」，而非強制斷頁邏輯。
- 為避免臆測，以 Node 腳本自 .vue 檔抽出真實 CSS（DocumentBody / ChopSection / PrintPreview
  ＋ quill.core.css ＋ bootstrap 全域重置）組出靜態重現頁，用無頭瀏覽器量測實際版面。
  量得：flow 內容高 1033px、核章欄底端相對 flow 頂 999px、引擎 usableBottom 1034px
  → 印證「預覽判 1 頁」且餘裕極小（近滿版）。
- 定位根因：`.preview-scroll .preview-doc-body .editor-body` 因 `.preview-doc-body` 與
  `.editor-body` 是同一元素、卻用子孫組合子而**從未生效**，使 `.editor-body` 的
  `padding: 30px 20px 96px` 在列印時仍在，造成 0.8cm 下移 + 2.5cm 尾部溢出
  （列印總高 1159px > A4 1122px）。
- 修正 PrintPreview.vue：於 `@media print` 加入 `.preview-app .editor-body`
  的 `margin/padding: 0 !important`（含詳細中文註解說明成因與「同元素多 class」陷阱）。
- 驗證：vitest 列印＋幾何 21/21 綠；全套 277/281，4 筆 EditorToolbar 失敗以 git stash
  比對確認為既存、與本次無關（如實回報，未宣稱全綠）。
- 誠實回報限制：本機無頭 Edge 反覆卡死（--dump-dom / --print-to-pdf 均逾時），
  **瀏覽器層級的實際列印無法在此環境自動驗證**，請實機 Chrome 確認；
  並提醒若仍分頁，下一嫌疑為 Chrome 列印對話框的「頁首及頁尾」與「邊界」設定
  （會另吃 1～2cm 頁高，CSS `@page { margin: 0 }` 管不到頁首頁尾）。
- 文件同步：更新 print-preview-flow/SKILL.md §二（新增第 5 點與已修 bug 警語）、
  修正 SKILLS_INDEX.md 中過時的 beforeprint/getPageGapCm 敘述。
- 記憶更新：project_spec016_print_preview 改寫為現行 break-before 架構並記錄本次 bug 與通則。
