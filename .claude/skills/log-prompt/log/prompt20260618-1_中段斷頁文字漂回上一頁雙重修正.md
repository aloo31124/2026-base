2026-06-18 23:12:07

## git commit
[fix] #755 [文稿分頁].[中段斷頁] 修正已斷項目重算後文字逐字漂回上一頁（索引位移根因＋切點黏滯遲滯）。
- 根因：measureEditorLineBoxes 回傳的 startIndex 為「含 embed 佔位的絕對索引」，但 reconcileBreaks/DesiredBreak.index 期望「乾淨索引」→ 每輪重算斷點往後挪一格、新頁文字逐字漂回上一頁。
- paginationController.applyCharSplit：新增 toCleanIndex()，依各編輯器既有 embed 之 absIndex 將絕對索引還原為乾淨索引後再作 DesiredBreak.index；既有頁界讀取一次（readCurrentBreaks）供索引換算與遲滯共用。
- lineBoxMeasure：walker 改 FILTER_REJECT 略過頁界 embed 子樹，避免 Quill 行內 embed 的 ﻿ guard 文字節點被誤計為字元（污染索引）或誤判為視覺行框（污染切點）；embed 以 compareDocumentPosition 在文字之前計入 1 個絕對索引佔位＋撐高。
- lineSplitPlanner：planLineSplit 新增 lockedLineIndices，對已存在頁界的切點行下調越界門檻 KEEP_SPLIT_HYSTERESIS_PX(6px)，單向黏滯遲滯（可往前切更多、不可往後回退）。
- breakPlanner：MeasuredBlock 新增 lockedSplitLines 並透傳；控制器以既有 embed 乾淨索引比對行框首字標記之。
- 測試：新增 lineSplitPlanner/breakPlanner「黏滯遲滯」單元測試；vitest 220 綠（4 紅為既有 DI 測試、無關）。索引換算屬瀏覽器相依，需 npm run dev 驗收。
- 文件：更新 print-preview-flow SKILL，新增「中段斷頁收斂陷阱」一節。

## prompt 1
createPaginationController.applyCharSplit 中，
    // 對「跨所在頁底」的 splittable 項目逐字量測行框（昂貴，故僅量越界者）。
    const linesByBlock = new Map<string, MeasuredLine[]>()
    for (const b of blocks) {
      if (b.kind !== 'splittable') continue
      const page = pageOfTop(metrics, b.top)
      if (b.top + b.height <= usableBottomOfPage(metrics, page)) continue
      const el = elementById.get(b.id)
      const target = targets.get(b.id.split('#')[0])
      if (!el || !target) continue
      // 行框量測同樣扣掉本項目之上已套用間距（priorPush），與乾淨 block top 同基準。
      const mlines = measureEditorLineBoxes(target.quill, target.findBlot, el, containerTop, zoom, priorPush.get(b.id) ?? 0)
      if (mlines.length > 1) {
        b.lineBoxes = mlines.map((l) => ({ top: l.top, height: l.height }))
        linesByBlock.set(b.id, mlines)
      }
    }
會對 該 項目多行 跨頁進行斷頁的計算，
請判斷當前項目若已經在新分頁中，
則不在做斷頁計算，return。

避免 當前項目已經在新分頁中，
重新計算後又再將文字漂移回上一分頁中，
避免之前已經斷頁的分頁被修改。

## prompt 1 response
- 先全面追碼（breakPlanner / lineSplitPlanner / lineBoxMeasure / pageGeometry / pageBreakManager / flowableBlock / DocumentBody）釐清資料流：每個 b 其實是單一 <li>（id 如 descArea#3），b.top/height 為 compensateAppliedSpacers 後的乾淨幾何。
- 指出字面做法（measure 迴圈直接 return/continue）會反效果：該 <li> 無 lineBoxes → reconcileBreaks 刪掉既有 embed、planPagination 把整塊 <li> 甩到下一頁、並破壞 shift 累積。以 AskUserQuestion 讓使用者選層級，使用者選「計畫層遲滯（完整收斂）」。
- 續追發現「漂回上一頁」真正肇因在索引空間：measureEditorLineBoxes 的 startIndex 是含 embed 的絕對索引，reconcileBreaks 卻期望乾淨索引（pageBreakManager.spec 佐證）→ 逐輪 +1 後移，方向與症狀完全吻合。再以 AskUserQuestion 確認，使用者選「索引根因＋幾何遲滯（雙重）」。
- 實作四處：
  1) lineSplitPlanner.planLineSplit 新增 lockedLineIndices + KEEP_SPLIT_HYSTERESIS_PX(6px) 單向黏滯遲滯。
  2) breakPlanner.MeasuredBlock 新增 lockedSplitLines 並透傳 planLineSplit。
  3) paginationController.applyCharSplit 新增 toCleanIndex()（絕對→乾淨索引）套用於 DesiredBreak.index，並以既有 embed 乾淨索引比對標記 lockedSplitLines；import readCurrentBreaks/CurrentBreak。
  4) lineBoxMeasure 改 FILTER_REJECT 略過 embed 子樹（避開 Quill ﻿ guard 誤計）、以 compareDocumentPosition 正確計入 embed 絕對索引佔位＋撐高。
- 新增單元測試（lineSplitPlanner、breakPlanner「黏滯遲滯」案例）；vitest run 全量 220 綠、4 紅為既有 EditorToolbar DI 測試（無關）；vue-tsc 對改動檔零型別錯誤。
- 更新 skills/business-logic/print-preview-flow/SKILL.md 新增「六、中段斷頁收斂陷阱」一節；提醒索引換算/guard 跳過屬瀏覽器相依，需 npm run dev 連續重算實機驗收 embed 不再逐字後移。
- 末段附符合規範的 git commit 區塊。
