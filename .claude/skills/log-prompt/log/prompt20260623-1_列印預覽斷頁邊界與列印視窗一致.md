2026-06-23 09:15:46

## git commit
[fix] #755 [列印預覽].[斷頁一致] 改以「每引擎頁 break-after:page 強制落一張 A4」修正 Chrome 列印與 [列印視窗] 斷點/邊界漂移。
- 根因：列印分頁完全依賴瀏覽器自動裁切連續內容流（beforeprint 收 pageGap=0 + 每 29.7cm 自動切）。
  (1) flex 容器（a4-flow/content-section/pages-wrap/editor-body/a4-wrapper/preview-app）不跨頁分段；
  (2) 引擎以螢幕 px 定位、瀏覽器以實體 cm 裁切，次像素誤差逐頁累積 → 斷點與每頁上下邊界漂移；
  (3) <body> 預設 8px 外距未重設 → 內容整體錯位。spec FR-005/FR-006 要求的 break-after:page 從未實作。
- PrintPreview @media print 重寫：祖先鏈 flex→block 使可正確分段；每個引擎頁首塊
  [data-a4-page-start] 施 break-before:page 精準對齊；續頁頂端留白改 padding-top/embed height
  （margin 於強制斷頁處會被截斷）值取 var(--a4-print-page-top)；body{margin:0}、
  a4-pagination-root{min-height:0}（免尾端空白頁）；修正失效之 break-inside:avoid 選擇器（改後代）。
- paginationDecorator.SpacerTracker：spacer>0（＝某頁首塊）標記 data-a4-page-start，apply/clear 同步清除。
- paginationController.run：以 metrics.usableTopPx 寫入 .a4-flow 之 --a4-print-page-top（續頁頂端邊界＝螢幕一致）。
- DocumentBody：移除 beforeprint/afterprint/isPrinting/getPageGapCm 收 gap=0 之舊機制（不可靠、已被強制斷頁取代）；螢幕續用 pageGap 1.2cm。
- paginationController：移除已無用之 getPageGapCm/resolveConfig（回退至 opts.config）。
- 測試：docRules 134 綠、vite build 成功；四方像素對位須 npm run dev + 輸出 PDF 實機驗收。
- 文件：更新 print-preview-flow SKILL（§一/§二/§五）反映強制斷頁新架構。

## prompt 1
開啟[列印視窗]後，按下 列印icon 開啟的 chrom列印預覽視窗 與 原本視窗不一致，
請找到原因並修正，務必確保斷頁一致，也務必確保每頁的 [頁面間距] 與原本 [列印視窗] 一致。

## prompt 1 response
- 研讀 print-preview-flow SKILL、PrintPreview.vue、DocumentBody.vue、paginationController/Decorator/Geometry/BreakPlanner、pageBreakBlot 與 spec 016 全套規格。
- 比對 git 歷史（a0c99613a 初版 → d4abc3c 重構共用 DocumentBody → 未提交的 beforeprint 收 gap=0）。
- 定位根因：列印分頁完全交給瀏覽器自動裁切連續流，且全程無 spec 明定之 `break-after: page`（連重構前也沒實作）→ flex 容器不跨頁分段 + px↔實體 cm 次像素誤差逐頁累積 + body 8px 外距未重設，造成斷點/邊界漂移；且 beforeprint 收 pageGap=0 使列印與螢幕 1.2cm 間距外觀不同。
- 因「頁面間距一致」對列印有兩種互斥解讀且物理上紙張間無法留白，以 AskUserQuestion 向使用者釐清：列印間距期望、與實際觀察到的不一致症狀。

## prompt 2
（AskUserQuestion 釐清回覆）
- 列印間距：選「每頁=乾淨A4(建議)」——頁間 1.2cm 僅螢幕視覺，列印時每個引擎頁印成一張乾淨 A4，斷點與每頁上下左右邊界與視窗逐頁相同（符合 spec FR-005）。
- 實際症狀（複選）：「斷頁位置跑掉」+「每頁邊界跑掉」（非頁間空白問題）。

## prompt 2 response
- 確認方向＝以強制斷頁讓每個引擎頁精準落一張 A4，續頁頂端留白須用不被截斷的機制。
- paginationController.run：新增以 `metrics.usableTopPx` 寫入 `.a4-flow` 的 CSS 變數 `--a4-print-page-top`；並移除已無用的 `getPageGapCm`/`resolveConfig`（回退 opts.config）。
- paginationDecorator.SpacerTracker.apply/clear：對 `spacer>0`（＝某引擎頁首塊）標記/清除 `data-a4-page-start`。
- DocumentBody.vue：移除 `isPrinting`/`handleBeforePrint`/`handleAfterPrint`/`getPageGapCm` 與 beforeprint/afterprint 監聽；螢幕續用 pageGap 1.2cm。
- PrintPreview.vue `@media print` 重寫：祖先鏈 flex→block、`[data-a4-page-start]{break-before:page; margin-top:0!important; padding-top:var(--a4-print-page-top)}`、`.a4-page-break{break-before:page; height:var(--a4-print-page-top)}`、`body{margin:0}`、`a4-pagination-root{min-height:0}`、修正失效的 `break-inside:avoid` 後代選擇器。
- 更新 print-preview-flow SKILL §一/§二/§五。
- 驗證：docRules 全套 134 測試綠、vue-tsc 僅餘既有 tsconfig baseUrl 既存警告、`vite build` 成功。

## prompt 3
請完成後續動作

## prompt 3 response
- 重建 production bundle（含本次修正）並整理 `src/main/resources/static/assets/`：unstage 並刪除舊碼建置殘留（index-DhbJlOkf.css／index-QQdFMdDN.js）與中間版殘留（index-CeYDn5Xw.js），保留 index.html 實際引用之 index-DlQeak8W.js + index-QE0jrUhY.css。
- 再次跑 docRules 134 測試全綠、`vite build` 成功。
- 提醒：四方（編輯區↔預覽↔Chrome 列印↔A4）像素級對位屬瀏覽器相依，仍須 `npm run dev` + 輸出 PDF 實機驗收（重點為條列中段斷頁之行內 embed 強制斷頁）。
- 產出符合專案規範之 git commit 區塊（[fix] #755 [列印預覽].[斷頁一致] …）。
