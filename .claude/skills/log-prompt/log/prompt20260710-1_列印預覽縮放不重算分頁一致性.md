2026-07-10 09:25:00

## git commit
[fix] #755 [列印預覽].[縮放一致] 預設倍率改 100%，縮放不再觸發分頁重算以保持斷頁結果一致。
- 根因：`DEFAULT_ZOOM` 沿用範例 0.85 非 100%；且 `DocumentBody` 之 `watch(zoom)→schedule()` 於每次縮放重新量測分頁，不同 `transform: scale` 倍率下 `getBoundingClientRect` 次像素差異可能讓臨界斷點翻頁。
- `previewZoomControl.ts`：`DEFAULT_ZOOM` 0.85 → 1（100%，與實體 A4 同大）。
- `PrintPreview.vue`：CSS 初始 `--zoom` 同步改 1，避免掛載瞬間顯示跳動。
- `DocumentBody.vue`：移除 `watch(zoom)→schedule()`；縮放屬純視覺 scale、不改版面配置，分頁於開窗（100%）算定後凍結，各倍率下斷頁逐頁一致。保留引擎 `getZoom`/`unzoom` 供行距／內容變動等其他重算還原邏輯像素。
- 文件：同步更新 `skills/business-logic/print-preview-flow/SKILL.md` 與 `specs/016-print-preview/`（spec FR-010、data-model PreviewZoom、research R6）。
- 測試：docRules 單元 134 綠、vite build 成功；縮放後斷頁不動需 npm run dev 實機驗收。

## prompt 1
## [列印預覽].分頁斷行.縮放一致性 
[列印預覽]視窗，預設大小 100% ，
並且 必須MUST 確保大小縮放時，
不要再重新計算 分頁與斷頁，保持原本分頁斷頁的結果。
確保 縮放大小時，分頁斷頁  可保持一致。

## prompt 1 response
- 探查 `PrintPreview.vue`、`previewZoomControl.ts`、`DocumentBody.vue` 之縮放機制，確認根因：`DEFAULT_ZOOM=0.85` 非 100%，且 `watch(zoom)→schedule()` 每次縮放都重新量測分頁，次像素差異可能讓臨界斷點翻頁。
- 修改 `previewZoomControl.ts`：`DEFAULT_ZOOM` 改為 1（100%）。
- 修改 `PrintPreview.vue`：CSS 初始 `--zoom` 同步改 1。
- 修改 `DocumentBody.vue`：移除 `watch(zoom)→paginationController.schedule()`，縮放不再觸發分頁重算，保留 `getZoom`/`unzoom` 供其他情境重算使用。
- 同步更新 `skills/business-logic/print-preview-flow/SKILL.md`（§一 zoom 契約、§三 previewZoomControl 職責）與 spec 016 之 `spec.md`（FR-010、PreviewZoom）、`data-model.md`、`research.md`（R6）。
- 驗證：`vitest run src/docRules` 18 檔 134 測試全綠；`npm run build` 成功。
