2026-07-10 10:15:00

## git commit
[feature] #755 [文稿編輯區].[縮放一致] 新增右下角縮放控制，預設 100%、純視覺 scale 不觸發分頁重算、各倍率斷頁一致。
- 新增 `Frontend/src/docRules/editorZoomControl.ts`：縮放純運算（夾限 50–200、步進 ±10、預設 100、頁寬換算 (clientWidth−48)/頁寬、標籤格式化）＋ 7 條單元測試。
- 新增 `Frontend/src/components/EditorZoomControl.vue`：右下角浮動縮放 bar，樣式與互動移植 `styleExamples/右下角比例縮放icon.html`（−/＋、選單頁寬/滑桿/預設 chip、收合藥丸、主色 #4f85a3 與 DraftList 對齊）。
- `DocumentEditor.vue`：持有 zoomPct/isFitWidth 狀態、`:zoom` 傳入 DocumentBody；頁寬模式於 window resize 重算；僅有作用中創稿時顯示。
- `DocumentBody.vue`：新增雙層縮放容器——外層 `.a4-zoom-sizer` 依 `--editor-zoom` 補償版面佔位（寬 CSS calc、高 ResizeObserver×倍率），內層 `.a4-zoom-scale` 做 transform: scale；`.editor-body`/`.a4-wrapper` 改 block＋margin auto 置中（防 >100% 左緣不可捲）；暴露 getScrollEl/getPageWidthPx 供頁寬換算；唯讀（列印預覽）不掛 is-zoomable、@media print 不受影響。
- 原因：MUST 縮放不改斷頁——不採範例之 CSS zoom（會真正重排版面、折行點次像素漂移），改用 transform（版面恆 100%），編輯中重算經引擎既有 getZoom/unzoom 還原邏輯像素 → 與 100% 量測完全一致；縮放本身無 watch(zoom)→schedule，不觸發重算。
- 同步新增 `skills/business-logic/editor-zoom-control/SKILL.md` 並更新 `skills/SKILLS_INDEX.md`。
- 測試：vitest 227 綠（4 紅為既有 DI 測試、無關）、vite build 成功；縮放後斷頁不動需 npm run dev 實機驗收。

## prompt 1
##[文稿編輯區] .分頁斷行.縮放一致性 
[文稿編輯區] ，預設大小 100% ，
並且 必須MUST 確保大小縮放時，
不要再重新計算 分頁與斷頁，保持原本分頁斷頁的結果。
確保 縮放大小時，分頁斷頁  可保持一致。

[文稿編輯區] 大小縮放 bar 必須MUST 參考:
Frontend\src\styleExamples\公文製作布局_草稿模式.html
Frontend\src\styleExamples\右下角比例縮放icon.html

## prompt 1 response
- 探查列印預覽既有縮放機制（`previewZoomControl.ts`、`PrintPreview.vue` 之 `--zoom`/transform scale/`getZoom`/`unzoom`）與兩份樣式範例（右下角比例縮放icon.html 之 UI 結構、公文製作布局_草稿模式.html 之 CSS `zoom` 縮放手法），確認範例用 CSS `zoom` 會真正重排版面、不適合編輯區（輸入必觸發重算）。
- 新增 `Frontend/src/docRules/editorZoomControl.ts`（夾限 50–200、步進 ±10、預設 100、頁寬換算、標籤格式化之純函式）與對應單元測試 `editorZoomControl.spec.ts`（7 案例全綠）。
- 新增 `Frontend/src/components/EditorZoomControl.vue`：右下角浮動縮放 UI，逐一移植範例之互動（步進、選單頁寬/滑桿/預設 chip、收合藥丸、click-outside/Escape 關閉）。
- 改造 `DocumentBody.vue`：新增雙層縮放容器 `.a4-zoom-sizer`（依倍率補償版面佔位：寬 CSS calc、高 ResizeObserver 監看分頁根 offsetHeight × 倍率）＋ `.a4-zoom-scale`（純 transform: scale），確保 transform 不參與版面計算、縮放不觸發分頁重算；`.editor-body`/`.a4-wrapper` 由 flex 置中改 block + margin auto，避免 >100% 時左緣不可捲；暴露 `getScrollEl()`/`getPageWidthPx()` 供頁寬換算；唯讀（列印預覽共用）不掛 is-zoomable，@media print 不受影響。
- 改造 `DocumentEditor.vue`：持有縮放狀態（zoomPct/isFitWidth/fitZoomPct），`editorZoom` computed 傳入 DocumentBody 之 `:zoom`；window resize 時頁寬模式即時重算；僅有作用中創稿時顯示 EditorZoomControl。
- 新增 `skills/business-logic/editor-zoom-control/SKILL.md`（核心不變式、雙層容器原理、排錯對照表）並更新 `skills/SKILLS_INDEX.md` 索引。
- 驗證：`npm run test:unit` 227 綠（4 紅為既有 EditorToolbar DI 測試、與本次無關）；`npm run build` 成功。縮放後斷頁不動與捲動對位屬瀏覽器相依，待 `npm run dev` 實機驗收。
