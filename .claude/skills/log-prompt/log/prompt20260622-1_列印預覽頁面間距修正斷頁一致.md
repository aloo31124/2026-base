2026-06-22 14:46:29

## git commit
[fix] #755 [列印預覽].[頁面間距] 修正列印視窗頁面相鄰無間距，改為螢幕 1.2cm／送印切 0 對齊 A4 且斷頁一致。

- 根因：唯讀預覽硬寫 pageGapCm:0（為對齊實體 A4），副作用為螢幕頁與頁緊貼、無視覺間距，與文稿編輯區（1.2cm）不一致。
- paginationController：新增 getPageGapCm 即時覆寫選項與 resolveConfig()，run()/degrade() 改用之，使 pageGap 可隨送印狀態切換而不改斷點。
- DocumentBody：唯讀幾何統一為 { continuationHeaderCm: 0 }（gap 預設 1.2cm，與編輯區同源）；新增 isPrinting，於 window beforeprint 收 gap=0 並同步 run()、afterprint 還原 1.2cm。
- PrintPreview：翻頁步距以 backdropGapPx() 量測相鄰背景頁間距，精準落於下一頁；更新 @media print 註解。
- 原理：pageGap 只影響視覺間距與 spacer 位移、不影響落點 → 編輯區／螢幕預覽／Chrome 列印三方斷頁一致。
- 測試：docRules 全套 134 綠、vite build 成功；像素對位需 npm run dev 實機驗收。
- 文件：更新 print-preview-flow SKILL 與 SKILLS_INDEX。

## prompt 1
列印視窗 頁面之間沒有 間距，請操考 Frontend\src\view\DocumentBody.vue 文稿頁面列表，頁面間距離一致，並且必須MUST 確保斷頁一致。

## prompt 1 response
- 盤點架構：[列印預覽] PrintPreview.vue 直接共用 DocumentBody.vue 唯讀模式；唯讀先前硬寫 `pageGapCm:0`（stride=一張 A4，方便 Chrome 列印對齊），副作用為螢幕頁與頁緊貼、無視覺間距。
- 確認關鍵原理（spec 015 既有）：`pageGap` 只影響視覺間距／spacer 位移，**不影響斷點**（落點由每頁可用內容高度決定）→ 可在「不動斷點」前提下加回間距。
- 釐清 @media print 依賴：列印需 gap=0 才能每頁恰 29.7cm 對齊實體 A4；故採「螢幕 1.2cm、送印才收 0」策略，兩態斷點相同。
- 改 `paginationController.ts`：新增 `getPageGapCm?()` 選項與 `resolveConfig()`，於 run()/degrade() 把即時覆寫併入 config。
- 改 `DocumentBody.vue`：幾何統一 `{ continuationHeaderCm: 0 }`（gap 預設 1.2cm，與編輯區同源）；新增 `isPrinting` ref；window `beforeprint` 設 true 並同步 `run()`（gap→0）、`afterprint` 還原；唯讀才註冊／解除監聽。
- 改 `PrintPreview.vue`：新增 `backdropGapPx()` 量測相鄰背景頁間距傳入 `scrollByPage`，翻頁精準落點；更新 @media print 註解。
- 驗收：`vite build` 成功；docRules 全套 134 測試綠（含 paginationController/breakPlanner/lineSplitPlanner/pageGeometry/print）。像素對位屬瀏覽器相依，建議 npm run dev 實機輸出 PDF 比對。
- 文件：更新 print-preview-flow/SKILL.md、SKILLS_INDEX.md；同步更新 spec016 專案記憶（標記已實作＋本次 gap 修正）。
- 結果：螢幕預覽頁間距 1.2cm（與編輯區一致），編輯區↔螢幕預覽↔Chrome 列印三方斷頁一致，列印每頁仍對齊實體 A4，編輯區無回歸。

## prompt 2
（/log-prompt）記錄這次對話、產生 prompt log。

## prompt 2 response
- Glob 查 `log/prompt20260622-*.md` 無既有檔 → 當日流水號為 1。
- 取系統時間 2026-06-22 14:46:29 為時間戳。
- 依規格寫出本檔 `prompt20260622-1_列印預覽頁面間距修正斷頁一致.md`（git commit ＋ prompt 逐字 ＋ response 精要）。
