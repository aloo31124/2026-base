2026-06-13 08:30:00

## git commit
[fix] #755 [列印預覽] 重構為共用 [文稿頁面列表] 元件唯讀模式，修正預覽/Chrome 列印斷頁不一致。

- DocumentBody 新增 readonly 模式：Quill readOnly、不綁互動規則、不回拋工具列、不寫回 Store；唯讀幾何 pageGapCm:0 使每頁恰為一張 A4。
- PrintPreview 改為直接內嵌 <DocumentBody :readonly :zoom>（同元件同引擎同內容）→ 斷頁與編輯區結構性一致；移除 clone/snapshot/adapter/controller 舊路徑。
- @media print 加 #app{display:none} 移除上方選單與編輯區、@page A4 margin:0、隱藏背景層、break-inside:avoid → Chrome 列印每頁對齊實體 A4。
- DocumentEditor 開窗前 saveCurrentFields() 存回最新內容；列印 icon 無創稿停用。
- 精簡：刪除 5 個舊模組（previewSnapshot/adapter/controller/geometry 及 2 個測試）；docRules/print 僅留列印樣式純工具（printTrigger/lineSpacingControl/previewZoomControl/pageNavigationControl/featureStubNotice）。
- 測試：print 單元 10 綠、全量 217 綠、零回歸；vite build 成功編譯。

## prompt 1
請修正如下兩 bug 問題：

1.[列印預覽]視窗 與 [文稿編輯區] 分頁斷頁沒有一致，須修正。
請重新調整架構，重構 refactor，
將 [文稿頁面列表] 中的 [文稿編輯區] 新增唯獨模式，不須要有標籤。
將 [列印預覽]視窗 分頁直接 conponent元件共用 [文稿頁面列表] ，
達到兩者一致，並精簡程式碼。

2.[chorm預覽列印]分頁斷頁不一致修正。
[chorm預覽列印] 呈現列印畫面時，將 上方選單移除，不可顯示。
並將 [文稿頁面列表] 中的 [文稿頁面] 內所有內容
能準確轉換為剛好 A4頁面大小 讓 [chorm預覽列印] 完整呈現一致。

## prompt 1 response
- 診斷：舊版以 clone DOM 另跑一套分頁引擎必然漂移；Chrome 列印因背景層/頁間距未對齐實體 A4。
- 重構方案：[列印預覽] 直接內嵌同一個 DocumentBody 元件（唯讀）→ 同源、同引擎、同內容結構 → 斷頁結構性一致。
- DocumentBody 改動：新增 `readonly` prop；唯讀時 Quill readOnly:true、不綁互動規則、不回拋工具列、不寫回 Store；保留純渲染綁定（排版不變）。分頁幾何唯讀改為 pageGapCm:0（stride=一張 A4）。加 zoom prop 與 watch 使縮放不影響斷點。
- PrintPreview 改寫：移除 teleport-based clone/snapshot/adapter 舊路徑；改為 <DocumentBody :readonly :zoom> 直接內嵌。@media print 加 #app{display:none}（移除編輯區+選單）、@page A4、隱藏 chrome/背景層、break-inside:avoid（區塊不切）。
- 精簡：刪除 previewSnapshot/previewPaginationAdapter/printPreviewController/printGeometry（舊 clone 路徑）及其 2 個測試（smoke/snapshot）→ docRules/print 僅留 5 個列印樣式純工具。
- 驗證：print 單元 10 綠（lineSpacing/pageNav/stub）、全量 vitest 217 綠 4 失敗（既有 EditorToolbar DI 測試，無回歸）、vite build 成功（875 modules）。
- 文件：已更新 print-preview-flow SKILL.md 反映新架構（共用元件、不再 clone、pageGap 語義釐清）。
