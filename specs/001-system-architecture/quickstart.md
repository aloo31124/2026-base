# Quickstart：系統架構設計

## 目錄

- [建置](#建置) — 單一 Gradle 命令
- [開發](#開發) — 前後端啟動

## 建置

`cd backend && ./gradlew build`（Windows 使用 `gradlew.bat build`）。

## 開發

後端：`backend/gradlew.bat bootRun`；前端：`cd frontend && npm run dev`。

